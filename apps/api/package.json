{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn src.main:app --reload --port 8000",
    "build": "echo 'Python backend requires no build step'",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "lint": "ruff check src/",
    "format": "black src/"
  }
}
